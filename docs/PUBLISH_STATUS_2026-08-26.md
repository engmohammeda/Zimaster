# 📤 حالة النشر + إصلاح بث الإعلانات — 2026/08/26

> **الفرع:** `arena/01a03c43-zimaster`
> **الهدف:** ١) علامة واضحة تدل على أن الدروس رُفعت فعلاً للسحابة، ٢) إصلاح «بث إعلان / نشر عبارة لا يعمل»، ٣) إعادة تصميم شاشة أدوات المطور.

---

## 1) ملخص تنفيذي

| المحور | قبل | بعد |
|--------|-----|-----|
| هل الدرس مرفوع؟ | لا يوجد أي مؤشر — لا في الشاشة ولا في البيانات | حقل `publishedAtMillis` + `publishedDocId` لكل درس، شارة لكل درس ولكل كورس، وعدّاد عام |
| رسالة فشل النشر | نص خام `PERMISSION_DENIED…` **بلون أخضر** (يبدو نجاحاً) | سطر نتيجة موحّد: أخضر = نجاح، أحمر = فشل + السبب + الحل |
| سبب رفض السحابة | مجهول تماماً | بطاقة «تشخيص الاتصال والنشر» + زر فحص حيّ (كتابة تجريبية ثم حذفها) |
| نشر كل الدروس | يقول «تم النشر» حتى لو فشل رفع **كل** الدروس | يعطي العدد الحقيقي للنجاح/الفشل + أول خطأ مترجماً |
| وضع المطور | يضيع بعد كل إعادة تشغيل (لم يكن محفوظاً) | محفوظ في الحالة (`developerUnlocked`) |
| إغلاق الإعلان | يعود للظهور عند كل إقلاع | محفوظ (`dismissedAnnouncementId`) |
| قواعد Firestore | `request.auth.token.email.matches(...)` على حساب ضيف = خطأ تقييم يُترجم كرفض غامض | فحص `!= null` قبل المقارنة |
| **الزر يعلق على «جارٍ البث…» للأبد** (لقطة 06:54) | `await()` على كتابة Firestore لا ينتهي بلا اتصال | مهلة 25 ثانية `timedCloud()` على كل عملية تنتظرها الواجهة → فشل واضح بدل تعليق |
| أزرار بطاقة النشر متداخلة/مقتطعة (لقطة 06:53) | صفّا أزران بنصوص طويلة تلتف وتُقص | تسميات أقصر + `maxLines=1` + اقتطاع منسّق وارتفاع موحّد |

---

## 2) علامة «تم الرفع»

**مصدر الحقيقة:** معرّف مستند Firestore = `{courseId}_lesson_{lessonNo}` — نفس القاعدة في
`CloudSync.publishLessonToCloud` وفي `upload_lessons.py`، ومجموعة الآن في دالة واحدة
`CloudController.lessonDocId(lesson)`.

**كيف تُملأ الشارة:**

| الطريق | متى |
|--------|-----|
| نشر درس / دفعة / «نشر الكل» | فور نجاح كتابة المستند |
| دروس وصلت من السحابة أثناء المزامنة | بالتعريف هي موجودة في السحابة |
| زر **«تحقق من السحابة»** | يقارن كل الدروس المحلية بفهرس `/lessons` الحقيقي (يشمل ما رفعه سكربت البايثون)، ويضع الشارة أو يمسحها |
| مزامنة التقدم بين جهازين | `StateMerger` يأخذ أحدث طابع زمني ولا يفقده أبداً |

**أين تظهر:**
- أعلى بطاقة النشر: شارة `مرفوع ✓` أو `x/y مرفوع` + شريط تقدم + تفصيل لكل كورس.
- شريط الحالة السريع: `دروس مرفوعة / عبارات / طلاب`.
- داخل الكورس (للمسؤول فقط): أيقونة لكل درس (`TaskAlt` أخضر = مرفوع، `CloudOff` = غير مرفوع) وسطر حالة للكورس.

> الفحص التلقائي يحدث **مرة واحدة في الجلسة** عند أول فتح للشاشة حتى لا يُنزَّل فهرس
> الدروس مع كل فتح (Firestore على أندرويد لا يدعم اختيار حقول، فيُنزَّل المستند كاملاً).

---

## 3) لماذا كان «بث الإعلان» لا يعمل

الأسباب الأربعة التي عولجت:

1. **لا جلسة سحابية** — `postAnnouncement` كانت تكتب بلا ضمان وجود حساب (إذا كانت
   المزامنة متوقفة لم يُنشأ حساب ضيف). الآن `ensureCloudSession()` قبل أي كتابة.
2. **صلاحية محلية ≠ صلاحية سحابية** — كود `ADMIN2026` يفتح الواجهة فقط؛ قواعد
   Firestore لا تقبل إلا حساب المالك أو دور `admin` في `/users/{uid}`. الشاشة الآن
   تعرض هذا صراحة (`isLocalOnlyAdmin`).
3. **القواعد لم تُنشر** — أي مسار غير معرّف يسقط في `deny`. زر «فحص الآن» يميّز هذه
   الحالة فوراً برسالة `PERMISSION_DENIED` مترجمة.
4. **الرسالة الخضراء** — الفشل كان يُعرض بلون النجاح، فيظن المستخدم أن الزر ميت.

```bash
# إذا قال الفحص PERMISSION_DENIED:
firebase deploy --only firestore:rules
firebase deploy --only firestore:indexes
```

---

## 4) الملفات المتغيّرة

| الملف | التغيير |
|-------|---------|
| `data/Models.kt` | `publishedAtMillis` / `publishedDocId` / `isPublishedToCloud` |
| `data/Persistence.kt` | نفس الحقلين في `LessonDto` + `dismissedAnnouncementId` + `developerUnlocked` في `ProfileDto` |
| `data/Mappers.kt` | تمرير الحقلين في الاتجاهين |
| `data/StateMerger.kt` | دمج شارة الرفع + الإعلان المُغلق بلا فقدان |
| `cloud/CloudSync.kt` | `fetchCloudLessonIndex` · `fetchRoleDoc` · `probePublishPermission` · تحقق من نص الإعلان |
| `viewmodel/CloudController.kt` | `lessonDocId` · `applyCloudMarks` · `verifyCloudLessons` · `probePublishPermission` · `describeCloudError` · نشر دقيق النجاح/الفشل |
| `viewmodel/AppViewModel.kt` | حالة النشر + الحفظ/الاستعادة + الواجهات العامة |
| `ui/screens/DeveloperToolsScreen.kt` | إعادة تصميم كاملة |
| `ui/screens/CourseScreen.kt` | شارة الرفع لكل درس ولكل كورس (للمسؤول) |
| `firestore.rules` | تأمين `isSuperAdminToken()` ضد الحساب الضيف |

كل حقول الحالة الجديدة **اختيارية بقيم افتراضية**، فالنسخ الاحتياطية القديمة تُقرأ بلا فقدان
(`ignoreUnknownKeys = true`).

---

## 5) استوديو المنهج المخصص (اتفاق النقاش 2026-08-26)

القرارات المتفق عليها: **المسؤول فقط** ينشئ · الإدخال **يدوي + لصق JSON من وكيل AI** ·
**بلا قوالب** · النشر **بزر النشر الحالي**.

| المكوّن | المكان | الوظيفة |
|---------|--------|---------|
| `createCustomCourse` | `ImportController` | كورس فارغ بمستوى أكاديمي أو مسار تخصصي جديد (`custom_<ts>` معرّف ثابت) |
| `addManualLesson` | `ImportController` | درس يدوي (عنوان/ملخص/نص EN/AR) |
| `importJsonIntoCourse` | `ImportController` | فكّ JSON (درس/مصفوفة/غلاف lessons) وتوجيهه لمنهج محدد متجاهلاً course_id النصي |
| زر «إضافة منهج مخصص» | `LevelsScreen` | حوار: اسم + نوع + مستوى (أكاديمية أو مسار جديد باسم حر) + هدف |
| «درس يدوي» + «لصق JSON من AI» | `CourseScreen` | يظهران للمسؤول في أي كورس (فارغ أو مأهول) |

**لماذا يصل للطلاب بلا كود إضافي؟** سلسلة النشر/المزامنة الحالية لا تعرف شيئاً عن
«الكورسات الرسمية»: `publishAllLocalLessonsToCloud` يرفع أي درس، و`importLessons`
على جهاز الطالب ينشئ الكورس والمسار التخصصي تلقائياً من الـ metadata — فالمنهج
المخصص يسافر بنفس الأنابيب المجرّبة، وتظهر عليه شارة «تم الرفع» تلقائياً.

**تحقق من عمل الوكيل السابق:** إصلاح G1 («استمع للنص كاملاً» في `ReadingBlocks.kt`)
وبروتوكول المفاجآت في `docs/BLOCKS_GUIDE.md` موجودان ومدموجان عبر PR #6.

---

## 6) مسار الهدف التطبيقي — القصة اليومية نحو هدف المتعلم (نقاش 2026-08-26)

قرارات النقاش: **استبدال كامل** للقصة العشوائية بقصة الهدف · **الـAI يبني السياق
تدريجياً** بسؤال يومي · **≤3 كلمات مستحقة** وبشرط خدمة الموقف · **اختبار إثبات
موقفي (٣ أسئلة، اجتياز ≥٢)** وحده يقدّم المرحلة · الأكاديمي يبقى منفصلاً في خطة اليوم.

| الملف | التغيير |
|-------|---------|
| `data/Models.kt` | `LifeGoal` (مراحل/سياق/مرحلة حالية) + `StageQuestion` |
| `data/StoryArchive.kt` | حقول القصة: `goalId/goalStage/contextQuestion*/contextAnswer` + `DailyStoryMaker.goalPrompt` |
| `data/GeminiStoryService.kt` | وضع «الهدف أولاً» في `generate` + `generateStageQuiz` بنوع `QuizResult` |
| `viewmodel/StoryController.kt` | `ensureGoalExists` (هدف المتعلم الأول المعلَن) · `createGoal/setActiveGoal` · `saveContextAnswer` · `requestStageQuiz/submitStageQuiz` |
| `ui/screens/DashboardScreen.kt` | شريط الهدف في بطاقة القصة + سؤال السياق اليومي + زر «إثبات المرحلة» + حوارا الأهداف والاختبار |
| `Persistence/Mappers/StateMerger` | حفظ الأهداف وحقول القصة + دمجها بين الأجهزة (الأعلى مرحلة يفوز) |

القصة القديمة (بدون `goalId`) تستمر بالعمل كما كانت — الحقول الجديدة افتراضية.

---

## 7) اكتشافية المنهج المخصص + إعادة تصميم «المزيد» (2026-08-26)

**مشكلة المستخدم:** ينشئ المنهج المخصص ثم «لا يجده» ضمن المستويات.
العلاج في `LevelsScreen`:
1. قسم مثبّت أعلى القائمة «مناهجي المخصصة 🎯» يعرض كل كورس `custom_*`
   بعدد دروسه واسم مستواه ويفتحه مباشرة.
2. بعد الإنشاء يُفتح المستوى المستهدف تلقائياً (`onCreated → expandedLevel`)
   ليرى المستخدم أين وُضع منهجه فوراً.

**إعادة تصميم شاشة «المزيد»** (`MainActivity.MoreSheetContent`) بهندسة معلومات مدروسة:
- هوية المتعلم أولاً (الاسم + سلسلة/XP/CEFR) بدل الشعار العام.
- حذف التكرار مع الشريط السفلي («مراجعة الكلمات» تبقت هناك فقط).
- أربع مجموعات بترتيب الرحلة: مسارك → ثبّت وقيّم → تقدّمك وحماسك → حسابك والنظام.
- مجموعة «النشر والإدارة 👑» تتصدر للمسؤول (مركز النشر + الاستيراد) بدل
  البوابة الذهبية الضخمة السابقة.
- بلاطات هادئة ملوّنة بدل التدرجات الصاخبة + شارة عدد القصص غير المقروءة.
