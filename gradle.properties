org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
# Enable Gradle build cache so agent builds get cache hits on unchanged template
# files. Cache is keyed by content hash (not path), so copies of this workspace
# reuse entries compiled into the worker Docker image via assembleDebug --build-cache.
org.gradle.caching=true
org.gradle.parallel=true

# ═══════════════════════════════════════════════════════════════════════════
# Supabase project credentials — Zimaster (project ref: eduobulbcwgcjruzphgc)
#
# These two values are PUBLIC by design:
#   • SUPABASE_URL      — the project's public API endpoint.
#   • SUPABASE_ANON_KEY — the publishable/anon key. It carries no privileges on
#                         its own; every request made with it is still filtered
#                         by Row Level Security (see supabase/migrations/).
#                         It is meant to ship inside the client APK.
#
# NEVER commit the service_role key or a database password here. Those live in
# GitHub Actions secrets (SUPABASE_SERVICE_ROLE_KEY / SUPABASE_DB_PASSWORD) or
# in the git-ignored local.properties / .env. See docs/SUPABASE_SETUP.md.
#
# Resolution order used by app/build.gradle.kts (highest wins):
#   1. environment variable          (CI: exported from GitHub secrets)
#   2. local.properties              (your machine — git-ignored)
#   3. this file                     (committed public defaults)
#   4. hard-coded dummy placeholder  (makes a credential-less build fail loudly
#                                     at runtime instead of at compile time)
# ═══════════════════════════════════════════════════════════════════════════
SUPABASE_URL=https://eduobulbcwgcjruzphgc.supabase.co
SUPABASE_ANON_KEY=sb_publishable_rc4aCFLVYRfvvWqMfS2VXg_k4MvTP0V
SUPABASE_PROJECT_REF=eduobulbcwgcjruzphgc
